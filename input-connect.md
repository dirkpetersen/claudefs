Create a simple Rust connectivity test module in crates/claudefs-connect/src/lib.rs.

Requirements:
1. Create Cargo.toml for the crate (use workspace dependencies from root Cargo.toml)
2. Create src/lib.rs with:
   - A public function: hello() -> String that returns "Hello from Rust"
   - 5 simple unit tests testing the hello function behavior
   - Full doc comments on the function and module

The module should be simple and straightforward.